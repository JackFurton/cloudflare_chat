# Chat Server Configuration with Cloudflare Tunnel
# Copy this file to .env and customize as needed

# Server configuration
PORT=5001
DEBUG=True

# Optional: Set to restrict allowed origins (comma-separated)
# CORS_ORIGINS=http://localhost:5001,https://example.com

# Security (generate with: python -c 'import os; print(os.urandom(24).hex())')
# SECRET_KEY=generaterandomkeyhere

# Cloudflare Tunnel is configured automatically
# No additional configuration is needed for basic usage